{
  "name": "xsight-app",
  "version": "1.0.0",
  "description": "AI Query Builder - Xsight Application with Firebase backend",
  "main": "public/index.html",
  "type": "module",
  "scripts": {
    "dev": "firebase emulators:start --only hosting",
    "serve": "firebase serve --only hosting",
    "build": "echo 'Static site - no build step required'",
    "deploy": "firebase deploy --only hosting",
    "deploy:full": "firebase deploy",
    "emulators": "firebase emulators:start",
    "emulators:ui": "firebase emulators:start --only firestore,auth,storage,hosting",
    "logs": "firebase functions:log",
    "test": "echo 'No tests specified'",
    "lint": "echo 'No linting configured'",
    "format": "prettier --write \"public/**/*.{js,css,html}\"",
    "validate": "firebase use --add && firebase projects:list"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/YOUR_USERNAME/xsight-app.git"
  },
  "keywords": [
    "ai",
    "firebase",
    "javascript",
    "query-builder",
    "agents",
    "machine-learning",
    "web-app",
    "file-upload",
    "authentication"
  ],
  "author": {
    "name": "Your Name",
    "email": "your.email@example.com"
  },
  "license": "MIT",
  "engines": {
    "node": ">=16.0.0",
    "npm": ">=8.0.0"
  },
  "dependencies": {
    "firebase": "^10.7.1"
  },
  "devDependencies": {
    "firebase-tools": "^13.0.0",
    "prettier": "^3.1.1",
    "http-server": "^14.1.1"
  },
  "prettier": {
    "semi": true,
    "singleQuote": true,
    "tabWidth": 2,
    "trailingComma": "es5",
    "printWidth": 100
  },
  "browserslist": [
    "> 1%",
    "last 2 versions",
    "not dead"
  ]
}{
  "name": "xsight-app",
  "version": "1.0.0",
  "description": "AI Query Builder - Xsight Application",
  "main": "public/index.html",
  "scripts": {
    "dev": "firebase serve",
    "build": "echo 'Static site - no build needed'",
    "deploy": "firebase deploy"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/YOUR_USERNAME/xsight-app.git"
  },
  "keywords": ["ai", "firebase", "javascript", "query-builder"],
  "author": "Your Name",
  "license": "MIT",
  "devDependencies": {
    "firebase-tools": "^13.0.0"
  }
}
